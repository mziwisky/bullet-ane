package ane.bulletphysics
{
	/**
	 * Inspired by Away3D.
	 * The namespace used to enclose properties and methods that should only be available within the library,
	 * which tends to be those related to AwayPhysics.
	 * This keeps the public API clean and prevents the calling of methods that should never be called manually.
	 */
	public namespace awp;
}